# Wishlist

Ideas y mejoras posibles, sin compromiso (quizá nunca). Código `WL-###` (nunca se reutiliza).
Si una idea se promueve, se borra de aquí y nace un `RM` nuevo.

**Formato:** título + idea en 1–2 líneas, sin campos.

---

## [WL-001] Alternador de tema claro/oscuro
Los tokens de la clase `.dark` ya están definidos en `globals.css`, pero nada los
activa. Falta `next-themes` y un control en el header; son pocas líneas cuando se quiera.
